# TODO: Improve Search and Sort System on Home Page

## Pending Tasks
- [ ] Modify app.py index route to return JSON for AJAX requests instead of HTML
- [ ] Update home.js to handle sort via AJAX (change sort buttons to trigger fetch instead of form submit)
- [ ] Remove the non-existent includes/character_results.html reference from app.py
- [ ] Expand search fields in app.py to include backstory and personality for robustness
- [ ] Test the AJAX updates to ensure smooth scrolling preservation
- [ ] Update TODO.md as steps are completed
